<nav class="navbar navbar-expand-lg navbar-ligth bg-dark fix-top">
  <a class="navbar-brand" href="<%= root_path %>">Home App</a>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <p class="text-white"><%= Complete.count%> de <%= Tarea.all.count%> tareas completas!</p>
      <% end %>
    </ul>
    <ul class="navbar-nav ml-auto">
      <li class="nav-item active">
        <% if user_signed_in? %>
          <%= link_to "Salir", destroy_user_session_path, method: :delete %>
        <% else %>
          <a class="nav-link" href="<%= new_user_session_path %>">Entrar</a>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
